<h1>Listy</h1>

<strong class="list-title">My Lists</strong>
 
<ul class="list-items">
  <% if @lists.length == 0 %>
    <p class="empty-list"> You don't have any lists- add some below!</p>
  <% else %>
    <% @lists.each do |list| %>
        <%= render('lists/show', :list => list) %>
    <% end -%>
  <% end %>
</ul>

<br>

<%= link_to 'Add New List', new_list_path %>