(function () {
  var id = "<%= @item.id %>";
  $('li#' + id).innerText("<%=h @item.content %>");
  $('lid#edit' + id).val("<%= @item.content %>");
  $('li#' + id).show();
  $('lid#edit' + id).hide();
})();